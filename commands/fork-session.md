# Fork Session

Fork the current Claude session and open it in a split terminal pane.

Execute the fork-session script:
```bash
~/.claude/scripts/fork-session.sh
```

After execution, inform the user that a new split terminal has been opened with a forked copy of this session. The forked session has its own independent history - changes in one won't affect the other.
